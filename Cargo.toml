[package]
name = "screeps-timing-annotate"
version = "0.1.0"
authors = ["William Archbell <william@archbell.com>"]
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["extra-traits", "full", "fold", "parsing"] }
quote = "1"
